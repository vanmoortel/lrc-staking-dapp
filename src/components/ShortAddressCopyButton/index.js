import { withStyles } from '@material-ui/core/styles';
import styles from './styles';
import ShortAddressCopyButton from './ShortAddressCopyButton';

export default withStyles(styles)(ShortAddressCopyButton);
